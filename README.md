# 道法自然

面向手机和桌面的个人洞察工具。底层结合八字、紫微、流年与传统历法，前端优先呈现现代生活语言、现实行动建议与分析限制。排盘计算保留在浏览器本地；用户主动点击 AI 智能详解时，当前可见详解文本才会发送到同源服务器。

## 功能

- 30 秒核心摘要、简洁/专业模式与六分区个人工作台
- 四柱八字、五行十神、紫微斗数及可折叠专业依据
- 今日五维决策卡、人生时间轴、年度卡与流月窗口
- 现代事项择日、五维关系图谱及跨术数一致/分歧提示
- 多人命盘库、个人节奏月历、自订事项与 ICS 日历导出
- 时辰校正、选项比较器与既往事件回测
- 梅花易数：经典时间起卦、两数起卦、体用与互变分析
- 奇门遁甲：时家转盘、九宫、九星、八门、八神、天地盘与逐宫详解
- 塔罗与 Petit Lenormand 标准牌组、本机抽牌与结构化解释
- DeepSeek 智能详解：服务端密钥、限流、缓存与模块化本地知识增强
- 匿名限时分享：隐藏姓名、出生日期、城市与完整命盘，支持撤销
- 双模式存储：默认本机保存；可选 PBKDF2 + AES-GCM 端到端加密同步

## 本地运行

```bash
python3 -m http.server 8765 --bind 127.0.0.1
```

打开 `http://127.0.0.1:8765/`。

## 测试

```bash
node --test tests/core.test.mjs
python3 -m unittest tests.test_ai_service
```

测试包含梅花经典基准、报数边界、奇门基准盘、出生资料边界、工作台规划层、关系图谱、决策工具、SEO 路由，以及匿名分享和加密同步服务端边界。

## AI 服务

```bash
DEEPSEEK_API_KEY=... python3 server/ai_service.py
```

服务默认只监听 `127.0.0.1:8788`，由同域反向代理转发 `/api/ai/*`、`/api/share/*` 与 `/api/sync/*`。密钥必须放在服务器环境变量或权限为 `600` 的环境文件里，不能写进网页。匿名分享及同步密文使用 SQLite 保存；生产环境由 systemd 的 `StateDirectory=tianji-ai` 提供持久目录。

## 私隐边界

- 默认命盘、资料库、日历与回测只在当前浏览器保存。
- 匿名分享接口会再次移除姓名、出生日期和城市等字段。
- 同步密码不会上传；服务器只收到 AES-GCM 密文、盐值与初始化向量。
- 当前版本不接入广告、第三方行为追踪或跨网站营销像素。
- 完整说明见 [privacy.html](privacy.html)。

## 智能解释边界

基础详解采用本地、可追溯的规则知识层，不依赖网络模型。AI 只能基于结构化排盘证据组织语言，不能改写排盘结果；涉及健康、投资、法律与人身安全时不得替代专业意见。

第三方来源与许可见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。
